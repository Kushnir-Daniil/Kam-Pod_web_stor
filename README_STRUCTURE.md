# Структура проєкту Kam-Pod_web_stor

Опис папок і файлів українською. Оновлюйте цей файл, коли додаєте нові модулі.

---

## Корень репозиторію

| Шлях | Призначення |
|------|-------------|
| `register/` | Вхід і реєстрація користувачів |
| `user/` | Екрани гравця: головна, квести, профіль, спільнота, плеєр квесту |
| `admin/` | Адмін-панель і конструктор квестів |
| `shared/` | Спільний код (Firebase, навігація, дані) |
| `img/` | UI-іконки та загальні зображення інтерфейсу |
| `builds/` | Готові Unity WebGL-білди ігор (по папках `game-1`, `game-2`…) |
| `content/` | Медіаконтент квестів (обкладинки, кадри історії/коміксу) |
| `README_STRUCTURE.md` | Цей файл — технічна карта проєкту |
| `README_ADMIN_GUIDE.md` | Інструкція для адмінів: як наповнювати квести без коду |

---

## `register/` — автентифікація

| Файл | За що відповідає |
|------|------------------|
| `index.html` | Сторінка входу (логін) |
| `register.html` | Сторінка реєстрації нового акаунта |
| `css/register.css` | Стилі форм входу/реєстрації |
| `js/register.js` | Обробка форм: показ пароля, виклик Firebase Auth, редірект на головну |

---

## `shared/` — спільне для всього сайту

| Файл | За що відповідає |
|------|------------------|
| `js/firebase.js` | Підключення Firebase (Auth + Firestore), експорт `auth` і `db` |
| `js/nav.js` | Нижня навігація (Головна / Спільнота / Квести / Профіль / Адмін) |
| `js/data/usersData.js` | Реєстрація, логін, вихід, ролі (`user` / `kazkar` / `admin`), коди запрошення |
| `js/data/questsData.js` | Модель квесту + статуси (`draft` … `archived`); поки IndexedDB, далі — Firestore |

---

## `user/` — частина гравця

### Сторінки

| Файл | За що відповідає |
|------|------------------|
| `home.html` | Головна: новини сайту, анонси, нові квести (контент сайту) |
| `community.html` | Спільнота / рейтинг (заглушка) |
| `quests.html` | Каталог квестів: пошук, таби, список карток |
| `quest.html` | Плеєр одного квесту: вкладки **Історія / Комікс / Гра** |
| `profile.html` | Профіль користувача (прогрес, досягнення; зараз + кнопка «Вийти») |

### Стилі

| Файл | За що відповідає |
|------|------------------|
| `css/user.css` | Загальний вигляд user-зони: фон, контейнер, нижня навігація, кнопка виходу |
| `css/quests.css` | Каталог квестів і плеєр (картки, таби режимів, CTA «ГРАТИ») |

### Скрипти

| Файл | За що відповідає |
|------|------------------|
| `js/quests.js` | Рендер списку квестів, пошук, перехід на `quest.html?id=…` |
| `js/quest-player.js` | Перемикання Історія / Комікс / Гра для вибраного квесту |
| `js/user.js` | Логіка user-сторінок (наприклад, вихід з профілю) |

---

## `admin/` — адмінка і конструктор

| Файл | За що відповідає |
|------|------------------|
| `dashboard.html` | Головна адмін-панелі, посилання на квести |
| `quests.html` | Список квестів для редагування + кнопка «Новий квест» |
| `quest-editor.html` | Конструктор квесту (мета / історія / комікс / гра) — каркас під форми |
| `css/admin.css` | Стилі адмінки |
| `js/admin.js` | Загальна логіка адмін-сторінок |
| `js/quest-editor.js` | Логіка конструктора (збереження полів квесту) |

Доступ до вкладки «Адмін» — `role: "admin"`. Вкладка «Казкар» — `role: "kazkar"` (конструктор своїх квестів).

---

## Ролі акаунта (Firestore)

Колекція `users/{uid}`:

| Поле | Значення |
|------|----------|
| `role` | `user` \| `kazkar` \| `admin` |
| `status` | `active` \| `blocked` |
| `isAdmin` | `true` лише для admin (сумісність) |
| `name`, `email`, `birthDate`, `coins`, `xp`, `createdAt` | профіль |

Квести казкаря зберігаються в підколекції:

`users/{kazkarUid}/quests/{questId}`

| Поле | Значення |
|------|----------|
| `authorId` | той самий `kazkarUid` |
| `status` | `draft` \| `pending_review` \| `rejected` \| `published` \| `archived` |
| `title`, `story`, `comic`, `game`, … | контент квесту |

Каталог гравців читає **collection group** `quests` (фільтр `status == published`).  
Черга адміна читає окрему колекцію `moderationQueue/{questId}` (щоб працювало між браузерами).

Колекція `inviteCodes/{CODE}` (код у UPPERCASE):

```json
{
  "role": "kazkar",
  "active": true,
  "maxUses": 50,
  "usedCount": 0
}
```

Правила: `firestore.rules`. Індекси collection group: `firestore.indexes.json` (або створити в Console, коли з’явиться посилання в помилці).

### Як додати код казкаря в Console

1. Firestore → **Start collection** → `inviteCodes`
2. Document ID: наприклад `FAWN2026`
3. Поля як у JSON вище
4. При реєстрації оберіть «Казкар» і введіть цей код

Існуючі користувачі з `isAdmin: true` при наступному логіні отримають `role: "admin"` автоматично.

**Важливо:** після оновлення Rules опублікуйте їх у Firebase Console. Старі квести з IndexedDB браузера в хмару самі не переїдуть — казкар має знову **Зберегти / Опублікувати** квест.

---

## `img/` — іконки інтерфейсу

Загальні картинки для форм, навігації, бейджів (монети, стрілки, замок тощо).  
**Не** кладіть сюди контент конкретних квестів — для цього є `content/quests/`.

---

## `builds/` — ігри (Unity WebGL)

Сюди кладуть **готові білди** (не сирі Unity-проєкти з репозиторію ігор).

Очікувана структура:

```
builds/
  game-1/
    index.html
    Build/
    TemplateData/
  game-2/
    …
```

У квесті поле `game.buildFolder` вказує на ім’я папки (`game-1`, `game-2`…).  
Плеєр відкриває гру в iframe з цієї папки (коли функціонал буде дописаний).

---

## `content/quests/` — медіа квестів

Контент для історій і коміксів (обкладинки, ілюстрації сцен).

Рекомендовано:

```
content/quests/
  papal-tower/
    cover.png
    story/
      page-01.png
    comic/
      scene-01.png
```

Шляхи до файлів зберігаються в даних квесту (`coverImage`, `story.pages[].image`, `comic.scenes[].image`).

---

## Модель квесту (коротко)

Один квест складається з трьох блоків:

1. **Історія** — сторінки: картинка + заголовок розділу + текст + цитата  
2. **Комікс** — сцени: картинка + репліки персонажів  
3. **Гра** — екран із кнопкою «ГРАТИ» → WebGL-білд (+ опційно геозона)

Деталі полів — у `shared/js/data/questsData.js` (`createEmptyQuest`).

---

## Як запускати локально

```powershell
cd D:\GitHub\Kam-Pod_web_stor
npx --yes serve -l 5500
```

Відкривайте з слешем у кінці папки, наприклад:

- http://localhost:5500/register/
- http://localhost:5500/user/quests.html

Без слеша (`/register`) стилі й скрипти можуть не підвантажитись.
